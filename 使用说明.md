# Ecliptica HUD 使用说明

> 这份文档只讲**怎么用**：每个数字什么意思、每项设置怎么改、出问题怎么排查。
> 项目介绍、编译方式、代码结构与验证情况请见 [README.md](README.md)。

---

## 目录

- [1. 三分钟上手](#1-三分钟上手)
- [2. 界面每一块是什么意思](#2-界面每一块是什么意思)
- [3. 怎么改设置](#3-怎么改设置)
  - [3.1 界面按钮能直接改的](#31-界面按钮能直接改的)
  - [3.2 只能改 config.ini 的：DPS 统计时间](#32-只能改-configini-的dps-统计时间)
  - [3.3 全部配置项速查](#33-全部配置项速查)
- [4. 快捷键](#4-快捷键)
- [5. 常见问题](#5-常见问题)
- [6. 进阶用法](#6-进阶用法)
- [7. 卸载](#7-卸载)

---

## 1. 三分钟上手

### 第一步：放好文件

把 `ecliptica-hud-c.exe` 放在**任意**你喜欢的目录（桌面、D 盘某个文件夹都行）。
不需要安装，不需要管理员权限，也不会写注册表。

### 第二步：启动

```bat
ecliptica-hud-c.exe
```

双击也行。启动后它会自己去这里找 VRChat 日志：

```
C:\Users\<你的用户名>\AppData\LocalLow\VRChat\VRChat\
```

在这个目录里挑**最后写入时间最新**的 `output_log*.txt` 跟着读。

### 第三步：确认它在工作

看面板**左下角的状态栏**，它一直在告诉你当前状态：

| 状态栏显示 | 含义 | 你要做什么 |
|---|---|---|
| `未找到 VRChat 日志` | 上面那个目录里没有日志 | 确认 VRChat 至少启动过一次 |
| `等待进入 Ecliptica` | 已连上日志，但你不在世界里 | 进世界即可，不用管 |
| `已在 Ecliptica，等待开局` | 在世界里，还没开始战斗 | 不用管 |
| `本局进行中 · <Boss名>` | 正在打 Boss | 不用管 |
| `本局进行中 · 间歇期 3` | 第 3 阶段之间的休息时间 | 不用管 |
| `演示模式` | 你用 `--demo` 启动的 | 不用管 |
| `打不开 --log 指定的文件` | 你指定的日志路径不存在 | 检查路径 |

**右下角**会显示它正在读哪个日志文件，比如 `output_log_2026-09-24_18-20-39.txt`。
只要这里有文件名、左下角不是"未找到"，就说明已经连上了。

### 面板没出现？

- 它是个**无边框的半透明窗口**，可能被其他窗口盖住了 —— 按 `Alt+Tab` 找找，
  或者看屏幕**水平居中、偏上四分之一**的位置（首次启动的默认位置）。
- 位置会被记住，下次还在上次的地方。

---

## 2. 界面每一块是什么意思

```
┌──────────────────────────────────────────────────────┐
│ ● Ecliptica HUD  战斗统计          [鼠标穿透中 · …]  ✕ │ ← 标题栏
├──────────────────────────────────────────────────────┤
│ [置顶][放大][缩小][变浓][变淡][穿透][日志]            │ ← 按钮行
├──────────────────────────────────────────────────────┤
│ 阶段 3 · Proto Colony            职业 Nekomancer      │ ← 阶段 / 职业
│ ▓▓▓▓▓░░░░░░░░░  Primal 25%   击倒 2  切换 7  印记 1/3 │ ← 进度 / 汇总
├──────────────────────────────────────────────────────┤
│ 本场 Beelzebub (P2)   时长 01:19   目标 xxx 00:07     │ ← 本场 Boss 行
├──────────────────────────────────────────────────────┤
│              阶段        本局        本场             │ ← 表头
│ 伤害      5,802      77,408      6,058               │
│ DPS        22.1        17.5        22.2               │
│ …                                                     │
├──────────────────────────────────────────────────────┤
│ 伤害来源（本场）                                      │
│ enemy · Kick Project                    29  x1       │
├──────────────────────────────────────────────────────┤
│ 事件日志 [全部][承伤][目标][阶段·Boss]         ▲ ▼    │
│ 22:09:50  离开世界                                    │
├──────────────────────────────────────────────────────┤
│ ◀ ▶ 本局 3/5          ◀ ▶ 本场 2/4          实时      │ ← 历史条
├──────────────────────────────────────────────────────┤
│ 本局进行中 · Amaziah              output_log_….txt    │ ← 状态栏
└──────────────────────────────────────────────────────┘
```

### 标题栏

- 正常时只有标题。
- **显示金色 `鼠标穿透中 · Ctrl+Alt+T 恢复`** 说明穿透开着（见第 4 节）。
- 右上 `✕` 关闭。

### 按钮行（7 个）

见 [3.1](#31-界面按钮能直接改的)。

### 阶段 / 职业 / 进度

- **阶段 N · 名称**：当前是第几个阶段、阶段叫什么。
- **职业**：本局你的职业。
- **进度条**：阶段进度百分比，同时显示进度对应的**阶段名**
  （Primal / Penumbral / Antumbral / Umbral / Eclipse / Eye of the Eclipse）。
- **击倒 N**：本局打死的 Boss 数。
- **切换 N**：本局敌方单位的目标（归属）切换总次数。
- **印记 N/M**：本阶段已经捡到 N 个 / 本阶段一共出现 M 个。

### 本场 Boss 行

`本场 <Boss名> (Pn)` —— `(P2)` 表示这是该 Boss 的第二形态。
同一个 Boss 的二阶段、三阶段算**同一场续战**，数据合并，不会打断统计。

右侧：

- `时长 mm:ss` —— 本场已经打了多久。
- `目标 <玩家> mm:ss` —— **该 Boss 当前锁定的目标是谁、锁了多久**。
- 如果显示 `目标 —`，说明日志里没有目标信息（见 [Q3](#q3-目标那一栏一直显示--是怎么回事)）。

### 三级统计表（重点）

三列的含义：

| 列 | 统计范围 |
|---|---|
| **阶段** | 只算当前这一小关 |
| **本局** | 从进入世界到离开，包含所有阶段和所有 Boss 战 |
| **本场** | 只算当前这一场 Boss 战（阶段里打的杂兵不算） |

九行的含义：

| 行 | 含义 |
|---|---|
| **伤害** | 你造成的伤害总量 |
| **DPS** | 最近一段时间内的每秒伤害，见 [3.2](#32-只能改-configini-的dps-统计时间) |
| **承伤** | 你受到的伤害总量 |
| **受击** | 你被击中的**次数** |
| **最大受击** | 单次受到的最大伤害 |
| **平均受击** | 承伤 ÷ 受击次数 |
| **承伤/秒** | 最近一段时间内每秒承受的伤害 |
| **死亡** | 死亡次数 |
| **印记** | 拾取到的印记数量 |

**为什么有的格子是 0 或 `—`？**

- `0` 是真的 0（比如这一场没受过击）。
- `—` 表示**这一级没有这个概念**。例如"本场印记"永远是 `—`，
  因为印记是按阶段结算的，不属于某一场 Boss 战。
- 阶段列在你还没进入任何阶段时也会全是 `0`。

### 伤害来源

按 **`谁 · 用什么招`** 把承伤拆开，右边是总伤害和命中次数。

标题会跟着当前状态变：

| 标题 | 说明 |
|---|---|
| `伤害来源（本场）` | 只看当前这场 Boss 战 |
| `伤害来源（本阶段）` | 当前阶段的承伤（不在 Boss 战时默认看这个）|
| `伤害来源（本局）` | 整局的承伤汇总（一局结束后默认看这个）|

不在战斗中也看不到当前阶段时，会自动显示整局汇总。

### 事件日志

按时间**倒序**（最新的在最上面），每条前面的时间就是日志里的时间。

- 四个过滤器按钮：**全部 / 承伤 / 目标 / 阶段·Boss**，点一下切换。
- 右键点面板**任意位置**也能循环切换过滤器（穿透关闭时）。
- `▲` 往上翻看更早的事件，`▼` 回到最新。
- 最多保留 **512 条**，超出的旧记录会被顶掉。

### 历史条 / 状态栏

- **左边的 ◀ ▶**：翻看**历史局**（`本局 3/5` = 第 3 局，共 5 局）。
- **中间的 ◀ ▶**：翻看**历史 Boss 战**（`本场 2/4`）。
- 右侧胶囊显示 **`实时`**（看当前局）或 **`历史`**（看历史局）。
- 翻看历史时，上面的三级统计表会整体切换成那一局的最终数据。

---

## 3. 怎么改设置

### 3.1 界面按钮能直接改的

按钮行的 7 个按钮，**点一下立即生效，并且自动记住**：

| 按钮 | 作用 | 范围 | 每次变化 |
|---|---|---|---|
| **置顶** | 窗口是否总在最前（开启时高亮）| 开 / 关 | — |
| **放大** | 整个界面放大 | 60% – 200% | +10% |
| **缩小** | 整个界面缩小 | 60% – 200% | −10% |
| **变浓** | 提高不透明度 | 40 – 255 | +15 |
| **变淡** | 降低不透明度 | 40 – 255 | −15 |
| **穿透** | 鼠标穿透开关（开启时高亮）| 开 / 关 | — |
| **日志** | 显示 / 隐藏事件日志面板（开启时高亮）| 开 / 关 | — |

> 按钮到顶/到底后就不再变化，不会越界。
> 鼠标停在按钮上时，**底部状态栏右侧**会显示这个按钮的说明。

另外：

- **拖动**：在面板空白处按住左键拖动即可移动窗口，位置自动记住。
- **过滤事件日志**：点日志面板标题栏的四个按钮。

### 3.2 只能改 config.ini 的：DPS 统计时间

**这是最常被问到的设置，而且界面按钮里没有它** ——
顶部的按钮位已经被「穿透」占用了，所以 DPS 统计窗口只能改配置文件。

#### 为什么需要改

DPS 是**滑动窗口**值，不是"总伤害 ÷ 总时长"。窗口越长数值越平稳，
窗口越短越能反映当下爆发。默认 **10 秒**。

#### 操作步骤

> ⚠️ **第 1 步不能省。**程序在退出时会把内存里的设置整个写回 `config.ini`。
> 如果你开着 HUD 改文件，退出时你的修改会被覆盖掉。

1. **完全退出 HUD**（点 `✕`，或按 `Ctrl+Alt+Q`）。
2. 找到 `config.ini`（位置见下方说明）。
3. 用**记事本**打开，找到这一行：

   ```ini
   stat_window=10
   ```

4. 改成你想要的值，只能填这六个之一：

   | 值 | 适合场景 |
   |---|---|
   | `3` | 看瞬时爆发，数字跳得很快 |
   | `5` | 短窗口，偏爆发 |
   | `10` | **默认**，比较平衡 |
   | `30` | 看稳定输出 |
   | `60` | 接近整场平均 |
   | `120` | 长线，长时间战斗才看得出变化 |

   > 填其它数字（比如 `15`）会被自动当作 `10` 处理。

5. 保存文件。
6. 重新启动 `ecliptica-hud-c.exe`。

#### config.ini 在哪？

有两种可能，按顺序判断：

1. **exe 同目录下有 `config.ini`** → 就用这个（便携模式）。
   适合绿色版：把 exe 和 config.ini 放一起，整个文件夹拷走设置也跟着走。
2. **否则** → 在按用户存放的目录里：

   ```
   C:\Users\<你的用户名>\AppData\Roaming\EclipticaHUD-C\config.ini
   ```

   快速打开：按 `Win+R`，粘贴下面这行回车：

   ```
   %APPDATA%\EclipticaHUD-C
   ```

   > 默认放这里是为了：① 装在 `C:\Program Files` 时普通用户也写得进去；
   > ② 同一台电脑的**不同 Windows 用户各存各的**，不会互相覆盖。

   > 注意别和上游 Rust 版 EclipticaHUD 的 `%APPDATA%\EclipticaHUD\` 弄混，
   > 本程序用的是带 `-C` 的那个目录。

#### DPS 到底是怎么算的

理解这一点，就知道为什么它有时和你心算的不一样：

```
DPS = 最近 N 秒内造成的伤害 ÷ 实际统计时长
```

其中"实际统计时长" = `min(N 秒, 本单元已经持续的时间)`。

- 战斗刚开始 2 秒时，分母是 2 秒而不是 10 秒，所以**开局的数字会跳得很厉害**，
  稳定下来之后才有参考价值。
- 三列 DPS 用**同一个窗口长度**，但**各算各的**：
  阶段列只统计本阶段的伤害，本场列只统计本场 Boss 战的伤害。
- 长时间没有任何可识别的日志事件时（比如站着发呆），时间基准不会前进，
  DPS 和"时长"会**停住不动**，一有事件就会继续。

### 3.3 全部配置项速查

文件本身长这样：

```ini
# Ecliptica HUD (C) config - UTF-8
always_on_top=1
alpha=210
scale=100
stat_window=10
show_event_log=0
click_through=0
win_x=538
win_y=71
ev_filter=0
# world_names: extra Ecliptica-family world names, separated by | or ,
world_names=
```

| 键 | 默认 | 取值 | 说明 | 建议改法 |
|---|---|---|---|---|
| `always_on_top` | `1` | 0 / 1 | 是否置顶 | 用「置顶」按钮 |
| `alpha` | `210` | 40 – 255 | 不透明度，越大越不透明 | 用「变浓/变淡」按钮 |
| `scale` | `100` | 60 – 200 | 界面缩放百分比 | 用「放大/缩小」按钮 |
| `stat_window` | `10` | 3 / 5 / 10 / 30 / 60 / 120 | **DPS 统计窗口（秒）** | **只能改文件**，见 3.2 |
| `show_event_log` | `0` | 0 / 1 | 是否显示事件日志面板 | 用「日志」按钮 |
| `click_through` | `0` | 0 / 1 | 鼠标穿透 | 用「穿透」按钮或 `Ctrl+Alt+T` |
| `win_x` / `win_y` | 自动 | 整数 | 窗口位置，`-1` = 自动居中 | 直接拖动窗口 |
| `ev_filter` | `0` | 0 / 1 / 2 / 3 | 日志过滤器：全部 / 承伤 / 目标 / 阶段·Boss | 点日志面板的过滤器按钮 |
| `world_names` | 空 | 字符串 | 额外识别的世界名，`\|` 或 `,` 分隔 | 见 [6. 进阶用法](#6-进阶用法) |

多数项目用界面按钮改就行，**只有 `stat_window` 必须手改文件**。

---

## 4. 快捷键

| 快捷键 | 作用 |
|---|---|
| `Ctrl+Alt+T` | **切换鼠标穿透**（全局有效，不用先点窗口）|
| `Ctrl+Alt+Q` | **退出程序**（全局有效）|

### 关于鼠标穿透

点「穿透」按钮或按 `Ctrl+Alt+T` 后，HUD 变得**对鼠标完全透明**：
点击、拖动、滚轮全部落到它下面的窗口上，可以照常操作游戏或桌面，
但 HUD 仍然正常显示数据。

**开启后窗口收不到任何鼠标消息，所以按钮就点不到了** —— 这是设计使然。
程序在标题栏常驻显示金色提示：

```
鼠标穿透中 · Ctrl+Alt+T 恢复
```

按 `Ctrl+Alt+T` 就能关掉穿透。状态会记进 `config.ini`，下次启动沿用。

> 什么时候用：HUD 挡住了游戏里要点的地方、或者挡住别的软件时，
> 开穿透就不用挪窗口了。

---

## 5. 常见问题

### Q1. 面板全是 0，是不是坏了？

先看状态栏：

- `未找到 VRChat 日志` → 程序没读到日志，去 [Q5](#q5-状态栏显示未找到-vrchat-日志)。
- `等待进入 Ecliptica` → 你不在世界里，进世界就会开始。
- `已在 Ecliptica，等待开局` → 在世界里但还没开打，正常。
- `本局进行中` 但还是 0 → 见 [Q2](#q2-dps-一直是-00)。

还有一种情况：**HUD 是从日志末尾开始读的**。如果你在进入世界之后才启动 HUD，
会错过之前的数据，但第一条战斗日志出现后就会自动开始统计。

### Q2. DPS 一直是 0.0？

按可能性排序：

1. **最近一段时间内没有造成伤害。** DPS 是滑动窗口值，窗口内没伤害就是 `0.0`。
   比如窗口 10 秒，你停下 10 秒不打，它就会归零 —— 这是正确行为，不是 bug。
2. **你还没进入任何阶段/战斗。** 伤害只在阶段或 Boss 战里统计。
3. **确认"伤害"那一行有数字。** 如果伤害有数而 DPS 是 0，说明是最近没输出。

想看更长时间的平均，把 `stat_window` 调大（见 [3.2](#32-只能改-configini-的dps-统计时间)）。

### Q3. 目标那一栏一直显示 `—` 是怎么回事？

**目标信息来自日志里的 `ownership of X transferred to Y` 行。**
如果当前世界的日志里根本没有这一行，程序就无法显示目标 —— 这不是解析问题，
是日志里没有这份数据，程序无法凭空推断。

官方世界会正常输出，目标栏会显示成 `目标 <玩家名> 00:12` 这样。

### Q4. 死亡次数好像不对？

程序对"死亡连刷"做了专门处理：一次死亡日志里可能**连刷几十行**
`Local controller dead`（实测 8 秒内 34 行），中间还会混进无关行。
程序要求"上次死亡之后必须重新出现活着的证据"，再加 3 秒静默期，
所以**一次死亡只计一次**。

如果你觉得仍不准，可以把那份日志发出来，用 `test_core.exe <日志>` 看统计摘要。

### Q5. 状态栏显示"未找到 VRChat 日志"

程序会在这几个位置找 `output_log*.txt`：

1. `C:\Users\<你>\AppData\LocalLow\VRChat\VRChat\`（取最新的一个）
2. 上一步没有时：exe 同目录 / 当前目录 / exe 的上一级目录

排查：

- 确认 VRChat **至少启动过一次**（没启动过就没有日志文件）。
- 打开 `%USERPROFILE%\AppData\LocalLow\VRChat\VRChat\` 看看有没有
  `output_log_xxxx-xx-xx_xx-xx-xx.txt`。
- 用 `--log` 直接指定文件（见 [6. 进阶用法](#6-进阶用法)）。

程序每 2 秒会重新找一次，所以**先开 HUD 再开 VRChat 也没关系**，它会自己连上。

### Q6. 状态栏显示"打不开 --log 指定的文件"

你用了 `--log` 但路径不对。注意：

- 路径**含空格时要用引号包起来**。
- `--log` 是**独占**的：文件打不开时程序**不会**偷偷去读别的日志，
  免得你看到一份完全无关的数据。改对路径或去掉 `--log` 即可。

### Q7. 我改了 config.ini，为什么没生效？

最常见的两个原因：

1. **改的时候 HUD 还开着。**程序退出时会把内存里的设置整个写回，
   把你的修改覆盖掉。**必须先完全退出 HUD，再改文件，再启动。**
2. **改错了地方。**确认你改的是程序实际使用的那个 `config.ini`
   （见 [config.ini 在哪](#configini-在哪)）。如果 exe 同目录下刚好有一个
   `config.ini`，程序会优先用它，而不是 `%APPDATA%` 里那个。

改完可以重新打开文件确认一下内容还在。

### Q8. 界面太大 / 太小

用「放大 / 缩小」按钮（60% – 200%）。

另外程序是 **DPI 感知**的：在 125% / 150% 缩放的显示器上会按比例放大，
不会像有些老程序那样被系统拉糊。所以"看起来偏大"有可能是系统缩放本身较大。

### Q9. 按钮点不动了！

你开了**鼠标穿透**。看标题栏是不是有金色的
`鼠标穿透中 · Ctrl+Alt+T 恢复`。按 `Ctrl+Alt+T` 关掉即可。

### Q10. 面板挡住了游戏里的东西

两个办法：

- **开穿透**（`穿透` 按钮 / `Ctrl+Alt+T`），点击会穿过去，面板继续显示。
- **挪走**：拖动面板，或者把「缩小」点几下。

### Q11. 想回看刚才那一局

用底部**左边的 ◀ ▶** 翻历史局，**中间的 ◀ ▶** 翻历史 Boss 战。
`本局 3/5` 表示当前看的是第 3 局、一共记录了 5 局。

程序保留最近 **8 局**、每局最多 **16 场 Boss 战**、**12 个阶段**的历史。

### Q12. 为什么看不到队友的 DPS？

**因为 VRChat 的日志里根本没有这个数据。**日志只记录**你自己**造成的伤害、
以及你自己受到的伤害。

这不是程序没做，是数据源的限制 —— 任何只读日志的工具都做不到。
（能做到的工具一定是注入或改包了，那不是本项目。）

---

## 6. 进阶用法

### 6.1 先学会打开命令行

下面的命令都要在**命令行窗口**里输入。三种打开方式任选：

| 方式 | 操作 | 特点 |
|---|---|---|
| **地址栏**（最推荐）| 打开 exe 所在文件夹 → 点一下顶部**地址栏** → 输入 `cmd` → 回车 | 命令行的工作目录就是该文件夹，相对路径最好写 |
| 运行框 | `Win+R` → 输入 `cmd` → 回车，然后 `cd /d "D:\你的路径"` | 通用 |
| 右键菜单 | 文件夹里 **Shift + 右键** → 「在此处打开 PowerShell 窗口」 | 功能相同，个别写法略异 |

三个能省很多事的技巧：

- **拖拽填路径** —— 把文件从资源管理器**拖进命令行窗口**，路径会自动填上，而且自带引号。
- **路径有空格必须加引号** —— `"D:\my hud\log.txt"`。不加引号会被拆成两个参数。
- **`Tab` 补全** —— 敲前几个字符按 `Tab` 自动补全文件名。

> ⚠️ **相对路径是相对"命令行的工作目录"，不是相对 exe。**
> 用「地址栏 → cmd」的方式打开时，工作目录正好是 exe 所在文件夹，最不容易出错。
> 拿不准就一律写**绝对路径**。

### 6.2 参数总表

| 参数 | 作用 | 会不会打开界面 |
|---|---|---|
| （什么都不加）| 自动定位日志，从**末尾附近**开始实时跟随 | 会 |
| `--demo` | 演示模式，用内置模拟数据 | 会 |
| `--log <文件>` | 回放指定日志文件（从头解析并继续跟随）| 会 |
| `--tail` | 起始位置改为「接近末尾」（最多回退 64 KB）| 会 |
| `--from-start` | 起始位置改为「从头读完整份」| 会 |
| `--help`、`-h`、`/?` | 弹出帮助窗口，然后退出 | **不会** |

> ✅ **大小写不敏感，单双横线都行。**
> `--demo`、`--Demo`、`--DEMO`、`-demo` 完全等价；`--LOG "路径"` 同样是合法的。
>
> ✅ **写错的参数会弹帮助。**比如 `--xyz` 或漏了文件名的 `--log`，
> 程序会直接弹出用法说明，而不是当作没看见 —— 免得你对着一个"看起来正常但行为不对"
> 的界面发愁。

### 6.3 逐个参数详解

#### `--demo` —— 演示模式

```bat
ecliptica-hud-c.exe --demo
```

不读任何日志文件，用内置的模拟事件循环播放。适合：

- 第一次装好，先看看界面长什么样
- **录屏 / 截图** —— 画面稳定可复现，录坏了随时重跑
- 改完配置想验证效果

**怎么确认真的进了演示模式：**看两处 —— 左下角状态栏显示 `演示模式`，
且**右下角不显示任何日志文件名**（因为没在读文件）。

> 如果右下角出现了 `output_log_2026-xx-xx_xx-xx-xx.txt` 之类的文件名，
> 说明演示模式**没生效**，程序正在读真实日志。检查参数是不是写成了
> `--Demo` 这类（现已支持）、或者被引号/输入法弄坏了。

#### `--log <文件>` —— 回放指定日志

```bat
ecliptica-hud-c.exe --log "D:\VRChatLogs\output_log_2026-09-24_18-20-39.txt"
```

从文件**开头**解析，解析完**继续跟随**这个文件的新增内容。适合作战复盘。

几个要点：

- **必须带路径**。写成 `--log` 后面没跟东西时，程序会**弹出帮助**（这是用法错误）。
- **独占**：如果这个文件打不开，程序**不会**偷偷退回自动定位，而是提示
  `打不开 --log 指定的文件` 并每 2 秒重试。
  这是刻意的 —— 否则你会看到一份完全不相干的数据还以为是对的。
- 以**只读**方式打开，不会修改或占用日志文件。
- 路径长度上限 260 字符，超长会被截断。
- **强烈建议写绝对路径**，见 [6.1](#61-先学会打开命令行) 的提示。

#### `--from-start` 与 `--tail` —— 控制"从哪里开始读"

决定第一次打开日志文件时从什么位置开始读。

| 命令 | 效果 |
|---|---|
| `ecliptica-hud-c.exe` | 从**接近末尾**开始，只看新产生的内容（默认，适合边玩边看）|
| `ecliptica-hud-c.exe --from-start` | 从**开头**把整份日志读一遍，再继续跟随 |

##### "接近末尾"到底有多近

程序会**最多往回退 64 KB**，然后从下一个换行符开始读：

| 日志大小 | 实际读取范围 |
|---|---|
| **小于 64 KB** | 几乎**整份**都会读（只跳过第一行）|
| **大于 64 KB** | 只读**最后 64 KB**，更早的内容被跳过 |

这个"预热"是有意为之：即使你在战斗打到一半才启动 HUD，也能靠最后 64 KB
把当前阶段、当前 Boss、累计伤害**重建出来**，而不是从一片空白开始。

##### `--from-start` 什么时候用

典型场景：**进游戏之后才想起来开 HUD**，想把这一局已经打过的部分也补上看一遍。

> **大日志提示**：一份 5 MB 的日志有 5 万多行，程序会分几次快速读完（约一两秒）。
> 期间面板数字会飞快地跳，属正常现象，追平后即稳定。

##### 注意：`--tail` 对 `--log` 同样生效

它不是"只管自动定位"——如果写在 `--log` **后面**，会把 `--log` 的回放也改成
"只读最后 64 KB"。这是最容易踩的坑，详见 [6.4](#64-组合用法与顺序陷阱)。

#### `--help` / `-h` / `/?` —— 看用法

```bat
ecliptica-hud-c.exe --help
```

弹出一个帮助窗口，点确定后程序直接退出，**不会打开 HUD**。

**参数写错时也会自动弹这个帮助**，例如：

```bat
ecliptica-hud-c.exe --xyz          :: 不认识的参数
ecliptica-hud-c.exe --log          :: --log 后面忘了写路径
```

这是有意设计的：以前这类错误会被**静默忽略**，程序照常启动去读真实日志，
界面上完全看不出哪里错了 —— 实测有用户把 `--demo` 打成 `--Demo`，
结果以为"演示模式坏了"。

### 6.4 组合用法与顺序陷阱

| 命令行 | 实际效果 |
|---|---|
| `ecliptica-hud-c.exe` | 实时跟随最新日志（默认）|
| `ecliptica-hud-c.exe --demo` | 演示模式 |
| `ecliptica-hud-c.exe --from-start` | 实时跟随，但先把当前日志从头读一遍 |
| `ecliptica-hud-c.exe --log "D:\log.txt"` | **从头完整回放**该文件 ✅ |
| `ecliptica-hud-c.exe --tail --log "D:\log.txt"` | **从头完整回放**该文件 ✅ |
| `ecliptica-hud-c.exe --log "D:\log.txt" --tail` | ⚠️ 只读该文件**最后 64 KB**，历史大量丢失 |
| `ecliptica-hud-c.exe --demo --log "D:\log.txt"` | 演示模式，`--log` 被忽略 |

**规则：`--log` 和 `--tail` 谁写在后面谁生效。**

`--log` 本身隐含了"从头开始"，而写在它后面的 `--tail` 会把这个行为改回"接近末尾"。

> ✅ **要回放文件时，把 `--log` 写在最后，或者干脆别加 `--tail`。**

##### 实测差距有多大

用一份 **5 MB 的真实日志**（53834 行）对比：

| 命令 | 识别出的局数 | 当前局伤害 | 阶段 |
|---|---|---|---|
| `--log BIG` | 5 局 | 77,408 | 阶段 6 · VRChat Hub |
| `--log BIG --tail` | 2 局 | 12,610 | 等待开始 |

差了将近 **6 倍**，而且阶段、击倒数、印记数全都不对。

> 反过来说：如果日志**小于 64 KB**（比如刚开的新会话），这两种写法看起来
> 几乎一样——因为"接近末尾"实际上就是从头读。**别因此以为顺序无所谓**，
> 等日志长大了就会踩坑。

##### 其它注意点

- **未知参数会弹出帮助**，不会静默忽略。
  注意：**把日志文件拖到 exe 图标上仍然是没有用的** —— 拖拽产生的参数是文件路径，
  不是 `-`/`--` 开头的选项，程序不会把它当成 `--log`。
  想拖拽回放请用 [6.5](#65-做成快捷方式--批处理) 的 `.bat` 方法。
- 参数**大小写不敏感**，`--demo` / `--Demo` / `-demo` 都可以。
- 只有 `--log` / `--tail` / `--from-start` 这三个跟"起始位置"有关的参数会互相覆盖，
  其余参数顺序随意。

### 6.5 做成快捷方式 / 批处理

#### 给"演示模式"建个桌面快捷方式

1. 右键 `ecliptica-hud-c.exe` → **发送到 → 桌面快捷方式**
2. 右键这个快捷方式 → **属性**
3. 把「**目标**」改成（注意 exe 路径后有一个空格）：

   ```
   "D:\你的路径\ecliptica-hud-c.exe" --demo
   ```

4. 确定。以后双击它就直接进演示模式。

#### 做个"拖拽即回放"的批处理（推荐）

和 exe 放在同一个文件夹，新建 `回放日志.bat`，内容：

```bat
@echo off
"%~dp0ecliptica-hud-c.exe" --log "%~1"
```

用法：**把日志文件拖到这个 bat 上**就会回放它。

- `%~dp0` = 这个 bat 自己所在的目录，所以 exe 路径怎么写都不会因工作目录出错；
- `%~1` = 拖进来的文件路径（自动带引号，含空格也没问题）。

#### 固定回放某一份日志

```bat
@echo off
start "" "%~dp0ecliptica-hud-c.exe" --log "D:\VRChatLogs\output_log_2026-09-24_18-20-39.txt"
```

### 6.6 配套小工具（需要自己编译）

`test_core.exe` 和 `preview.exe` **不随发行的 exe 一起提供**，要用源码编译：

```bat
mingw32-make test      :: 编译并运行全部逻辑测试
mingw32-make preview   :: 编译离屏预览工具
```

#### `test_core.exe` —— 看日志到底被解析出了什么

```bat
test_core.exe                               :: 不加参数 = 跑全部逻辑测试
test_core.exe "D:\VRChatLogs\output_log.txt" :: 加参数 = 回放并打印统计摘要
```

输出长这样：

```
lines=1552  events=415  accepted=375
ownership: parse 0 -> keep 0 (per-object, target really changed)
runs=1  in_run=0  stage_no=1  targets_total=0
all runs: deaths=1  kills=1  targets=0
run: dmg=3188 taken=453 hits=46 deaths=1 tokens=1 fights=1 stages=1
```

怎么读：

| 看到 | 说明 |
|---|---|
| `events=0` | 这份日志里**没有任何可识别的事件** —— 文件选错了，或者日志格式变了 |
| `lines` 很大但 `events` 很小 | 正常，日志里绝大多数是无关的系统消息 |
| `ownership: parse 0` | 这份日志没有目标信息，HUD 的目标栏会显示 `—`（见 [Q3](#q3-目标那一栏一直显示--是怎么回事)）|
| `runs=` | 识别出了几局 |
| `deaths=` / `kills=` | 全局死亡数与击杀数 |

排查"为什么面板没数据"时，这个工具比盯着 HUD 猜有效得多。

#### `preview.exe` —— 不开游戏渲染一张界面图

```
preview.exe  [日志] [缩放%] [日志面板0/1] [输出.bmp] [只回放前N行]
```

| 位置 | 默认 | 说明 |
|---|---|---|
| 日志 | 内置示例 | 传 `-` 或不传 = 用内置示例数据 |
| 缩放% | `100` | 允许 50 – 300 |
| 日志面板 | `1` | `1` = 带事件日志面板，`0` = 不带 |
| 输出文件 | `hud_preview.bmp` | 24 位 BMP |
| 只回放前 N 行 | `0` | `0` = 全部；填行号可渲染"打到一半"的状态 |

例子：

```bat
preview.exe
:: 内置数据 / 100% / 带日志面板 → hud_preview.bmp

preview.exe "D:\VRChatLogs\output_log.txt" 150 1 out.bmp 1970
:: 150% 缩放，只回放到第 1970 行，导出 out.bmp

preview.exe - 60 0 small.bmp
:: 内置数据 / 60% / 不带日志面板 → small.bmp
```

适合做教程配图，也适合在**没有显示器或远程环境**下检查排版。

> 注意：`preview.exe` 只是渲染一张图，**不会**弹出窗口、也不读 `config.ini`。

### 6.7 让程序识别别的世界名

程序内置只认官方世界名。如果你玩的是**同一套日志格式**的其它世界，
可以在 `config.ini` 里登记房间名：

```ini
world_names=世界名A|世界名B
```

用 `|` 或 `,` 分隔，**子串匹配**（大小写不敏感）。

> 即使完全不登记，只要日志里出现 `ECLIPTICA …` 系事件，程序也会自动开始统计 ——
> 只是进房那一刻不会立刻开局，要等第一条战斗日志。

---

## 7. 卸载

1. 关掉程序，删除 `ecliptica-hud-c.exe`。
2. 删除配置目录（可选，不删也不影响）：

   ```
   %APPDATA%\EclipticaHUD-C
   ```

3. 如果 exe 同目录下生成了 `config.ini`，也一并删掉。

程序不写注册表、不装服务、不动系统文件，删掉即干净卸载。

---

## 附：一句话备忘

| 我想…… | 怎么做 |
|---|---|
| 调 DPS 统计时间 | 退出程序 → 改 `config.ini` 的 `stat_window` → 重启 |
| 调透明度 / 缩放 / 置顶 | 点顶部按钮，立即生效 |
| 让鼠标点穿 HUD | 点「穿透」或 `Ctrl+Alt+T`；再按一次恢复 |
| 挪位置 | 拖动面板 |
| 只看承伤事件 | 点日志面板的「承伤」 |
| 回看上一局 | 底部左边的 ◀ |
| 不用进游戏看效果 | `--demo` |
| 复盘某份日志 | `--log "路径"`（写在最后，别加 `--tail`）|
| 补看这一局已经打过的部分 | `--from-start` |
| 退出 | `Ctrl+Alt+Q` 或点 `✕` |
